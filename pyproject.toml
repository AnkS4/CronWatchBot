[project]
name = "cronwatchbot"
version = "0.1.0"
description = "Manage urlwatch and crontab via Telegram"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-crontab>=3.3.0",
    "python-telegram-bot>=22.7",
    "pyyaml>=6.0.3",
]
