name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.14"

    - name: Install uv
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Cache uv dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/uv
          .venv
        key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
        restore-keys: |
          ${{ runner.os }}-uv-

    - name: Install dependencies
      run: |
        uv sync

    - name: Lint with ruff
      run: |
        uv run ruff check .
        uv run ruff format --check .

    - name: Type check with mypy
      run: |
        uv run mypy config/ helpers/ handlers/ main.py
      continue-on-error: true

    - name: Run unit tests
      run: |
        uv run pytest tests/unit/ -v --cov=config --cov=helpers --cov=handlers --cov-report=term-missing

    - name: Run integration tests
      run: |
        uv run pytest tests/integration/ -v

    - name: Generate coverage report
      run: |
        uv run pytest tests/ --cov=config --cov=helpers --cov=handlers --cov-report=term-missing --cov-fail-under=80

    # TODO: Add Codecov integration later if needed (codecov.io)

    - name: Test summary
      if: always()
      run: |
        echo "## Test Results" >> $GITHUB_STEP_SUMMARY
        echo "✅ All tests completed" >> $GITHUB_STEP_SUMMARY
